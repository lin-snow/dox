# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /v1/auth/redeem:
        post:
            tags:
                - AuthService
            description: |-
                RedeemPairingCode trades a one-time pairing code for a per-device bearer
                 token. The code is invalidated on success and rejected if expired or
                 already consumed.
            operationId: AuthService_RedeemPairingCode
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RedeemPairingCodeRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RedeemPairingCodeResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/todos:
        get:
            tags:
                - TodoService
            description: ListTodos returns all todos, newest first.
            operationId: TodoService_ListTodos
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListTodosResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - TodoService
            description: CreateTodo creates a new todo and returns it with server-generated fields.
            operationId: TodoService_CreateTodo
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateTodoRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Todo'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/todos/{id}:
        get:
            tags:
                - TodoService
            description: GetTodo returns a single todo by id.
            operationId: TodoService_GetTodo
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Todo'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        delete:
            tags:
                - TodoService
            description: |-
                DeleteTodo permanently deletes a todo (hard delete — no tombstones since
                 server is the single source of truth in thin-client mode).
            operationId: TodoService_DeleteTodo
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteTodoResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        patch:
            tags:
                - TodoService
            description: |-
                UpdateTodo applies a partial update to a todo. Fields absent in the request
                 are left unchanged.
            operationId: TodoService_UpdateTodo
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateTodoRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Todo'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateTodoRequest:
            type: object
            properties:
                title:
                    type: string
        DeleteTodoResponse:
            type: object
            properties: {}
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListTodosResponse:
            type: object
            properties:
                todos:
                    type: array
                    items:
                        $ref: '#/components/schemas/Todo'
        RedeemPairingCodeRequest:
            type: object
            properties:
                code:
                    type: string
        RedeemPairingCodeResponse:
            type: object
            properties:
                token:
                    type: string
                deviceId:
                    type: string
                deviceName:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        Todo:
            type: object
            properties:
                id:
                    type: string
                    description: Server-generated ULID, primary key.
                title:
                    type: string
                    description: Human-readable title.
                done:
                    type: boolean
                    description: Whether the todo is marked done.
                createdAt:
                    type: string
                    description: Creation time, UTC unix milliseconds.
                updatedAt:
                    type: string
                    description: Last update time, UTC unix milliseconds.
            description: Todo is a single todo item.
        UpdateTodoRequest:
            type: object
            properties:
                id:
                    type: string
                title:
                    type: string
                done:
                    type: boolean
tags:
    - name: AuthService
      description: |-
        AuthService is the only public surface (no Authorization header required).
         All other RPCs require a bearer token issued via RedeemPairingCode.
    - name: TodoService
      description: TodoService manages todos for the authenticated device.
