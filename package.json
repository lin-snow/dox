{
  "name": "dox",
  "private": true,
  "version": "0.0.0",
  "description": "Self-hosted personal todo app — monorepo root",
  "workspaces": [
    "apps/cli",
    "packages/*"
  ],
  "scripts": {
    "cli": "bun run apps/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
